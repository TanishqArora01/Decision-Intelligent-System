# Browser calls this URL (use proxy in local dev)
NEXT_PUBLIC_API_URL=/api/backend

# Next.js rewrites /api/backend/* → this host (app-backend default port)
BACKEND_URL=http://127.0.0.1:8400

# Docker: set NEXT_PUBLIC_API_URL=http://localhost:8400 and BACKEND_URL=http://app-backend:8400
